# Local Locks

This directory is for local coordination when multiple agents are authoring zone "shape" files in parallel.

- Lock files live in `locks/areas/` as `*.lock`.
- Lock files are intentionally not committed. See `.gitignore`.

