# DOCBOT-2 Doing

- Keep the dev blog bot roster + org chart consistent between `web_homepage/devlog.js` and `docs/devteam/`.
- Audit the "HTTPS" workflow docs for drift (cert issuance, renewal hook, smoke test).

