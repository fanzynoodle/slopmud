# AI-CHIEF-8 Plan

## Next

- Build an internal "AI toolbelt" service pattern:
  - low memory footprint by default
  - explicit rate limits and budgets
  - audit logs for actions taken
- Add basic automated content linting (typos, broken links/exits, forbidden phrases).

## Later

- Add optional player-facing AI helpers (strictly opt-in; minimal data retention).
- Improve moderation assist without automating enforcement decisions.

