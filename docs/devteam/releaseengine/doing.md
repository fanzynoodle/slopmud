# RELEASE-ENGINE-10 Doing

- Tighten release build determinism (locked deps, consistent toolchain, consistent flags).
- Add pre-deploy smoke tests that fail fast (ports, healthz, TLS when applicable).

