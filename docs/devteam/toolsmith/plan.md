# TOOLSMITH-8 Plan

## Next

- Create a "tiny tool" template (clap + tracing + config + low-memory defaults).
- Add a unified smoke-test runner that can run locally and in CI.

## Later

- Build an internal "service inspector" (health, limits, raft status) that fits in a few MB RAM.

